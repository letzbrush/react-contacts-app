import { PayloadAction } from '@reduxjs/toolkit'

export const requestFailureReducer = (
  state: unknown,
  action: PayloadAction<unknown>,
) => {
  // actionPayload is used in a different reducer
}
